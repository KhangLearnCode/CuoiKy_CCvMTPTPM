@{
    ViewData["Title"] = "Cửa hàng nước hoa";
}

<style>
    /* Custom CSS to match the image precisely */
    .sidebar-bg {
        background-color: #d1d5db;
        border-radius: 1rem;
    }
    .product-card {
        background-color: #f3f4f6;
        border-radius: 1rem;
        transition: transform 0.2s;
    }
    .product-card:hover {
        transform: translateY(-5px);
    }
    .tag {
        background-color: #e5e7eb;
        padding: 2px 8px;
        border-radius: 4px;
        font-size: 0.7rem;
        color: #4b5563;
    }
    .price {
        font-weight: 700;
        font-size: 1rem;
    }
    .old-price {
        text-decoration: line-through;
        color: #9ca3af;
        font-size: 0.8rem;
        margin-left: 8px;
    }
    .discount-badge {
        background-color: #ff0000;
        color: white;
        font-weight: bold;
        font-size: 0.7rem;
        padding: 2px 6px;
        border-radius: 4px;
        position: absolute;
        top: 10px;
        left: 10px;
    }
    .star-rating {
        color: #fbbf24;
        font-size: 0.8rem;
    }
    .review-count {
        color: #9ca3af;
        font-size: 0.8rem;
        margin-left: 4px;
    }
    /* Custom Scrollbar for the product grid */
    .product-grid-container::-webkit-scrollbar {
        width: 8px;
    }
    .product-grid-container::-webkit-scrollbar-track {
        background: #f1f1f1;
    }
    .product-grid-container::-webkit-scrollbar-thumb {
        background: #888;
        border-radius: 10px;
    }
    .product-grid-container::-webkit-scrollbar-thumb:hover {
        background: #555;
    }
    /* Modal styles */
    .modal-overlay {
        background-color: rgba(0, 0, 0, 0.5);
    }
    .custom-range::-webkit-slider-thumb {
        -webkit-appearance: none;
        appearance: none;
        width: 16px;
        height: 16px;
        background: black;
        cursor: pointer;
        border-radius: 50%;
    }
</style>

<div class="container mx-auto px-4 py-8">
    <div class="flex flex-col md:flex-row gap-6">
        <!-- Sidebar -->
        <aside class="w-full md:w-1/4 sidebar-bg p-6 h-fit sticky top-24">
            <h2 class="text-2xl font-bold mb-6">Bộ lọc</h2>
            
            <!-- Price Range -->
            <div class="mb-8">
                <div class="flex items-center justify-between mb-2">
                    <h3 class="font-semibold">Khoảng giá</h3>
                    <i class="fa-solid fa-chevron-down text-sm"></i>
                </div>
                <input type="range" id="priceRange" min="0" max="10000000" step="100000" value="10000000" class="w-full h-1 bg-black rounded-lg appearance-none cursor-pointer custom-range">
                <div class="flex justify-between text-xs mt-3">
                    <span>0đ</span>
                    <span id="priceLabel">10.000.000đ</span>
                </div>
            </div>

            <!-- Gender -->
            <div class="mb-8">
                <div class="flex items-center justify-between mb-4">
                    <h3 class="font-semibold">Giới tính</h3>
                    <i class="fa-solid fa-chevron-down text-sm"></i>
                </div>
                <div class="space-y-3">
                    <label class="flex items-center space-x-3 cursor-pointer">
                        <input type="checkbox" name="gender" value="Nam" class="form-checkbox h-4 w-4 rounded border-gray-300">
                        <span class="text-gray-700">Nam</span>
                    </label>
                    <label class="flex items-center space-x-3 cursor-pointer">
                        <input type="checkbox" name="gender" value="Nữ" class="form-checkbox h-4 w-4 rounded border-gray-300">
                        <span class="text-gray-700">Nữ</span>
                    </label>
                    <label class="flex items-center space-x-3 cursor-pointer">
                        <input type="checkbox" name="gender" value="Unisex" class="form-checkbox h-4 w-4 rounded border-gray-300">
                        <span class="text-gray-700">Unisex</span>
                    </label>
                </div>
            </div>

            <!-- Brands -->
            <div class="mb-8">
                <div class="flex items-center justify-between mb-4">
                    <h3 class="font-semibold">Thương hiệu</h3>
                    <i class="fa-solid fa-chevron-down text-sm"></i>
                </div>
                <div class="space-y-3" id="brandFilters">
                    <!-- Brands will be populated by JS -->
                </div>
            </div>
        </aside>

        <!-- Main Content -->
        <div class="w-full md:w-3/4">
            <!-- Toolbar -->
            <div class="flex flex-wrap items-center justify-between mb-6 bg-gray-200 p-4 rounded-xl">
                <div class="flex items-center space-x-4">
                    <h2 class="text-xl font-bold">Sản phẩm</h2>
                </div>
                <div class="flex items-center space-x-4 mt-2 md:mt-0">
                    <select id="sortSelect" class="bg-white border-none rounded-lg py-2 px-4 text-sm focus:ring-0 cursor-pointer">
                        <option value="default">Mặc định</option>
                        <option value="price-asc">Giá: Thấp đến cao</option>
                        <option value="price-desc">Giá: Cao đến thấp</option>
                        <option value="name-asc">Tên: A-Z</option>
                        <option value="newest">Mới nhất</option>
                    </select>
                    <div class="flex bg-white rounded-lg p-1">
                        <button class="p-1.5 px-3 rounded-md bg-gray-200"><i class="fa-solid fa-table-cells"></i></button>
                        <button class="p-1.5 px-3 rounded-md hover:bg-gray-100"><i class="fa-solid fa-list"></i></button>
                    </div>
                </div>
            </div>

            <!-- Product Grid -->
            <div id="productGrid" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                <!-- Products will be populated by JS -->
            </div>
        </div>
    </div>
</div>

<!-- Quick View Modal -->
<div id="quickViewModal" class="fixed inset-0 z-[100] hidden items-center justify-center p-4 modal-overlay">
    <div class="bg-white rounded-3xl max-w-4xl w-full overflow-hidden relative flex flex-col md:flex-row shadow-2xl">
        <button id="closeModal" class="absolute top-4 right-4 text-gray-500 hover:text-black z-10">
            <i class="fa-solid fa-xmark text-2xl"></i>
        </button>
        <div class="w-full md:w-1/2 bg-gray-100 p-8 flex items-center justify-center">
            <img id="modalImage" src="" alt="Product" class="max-h-96 object-contain" onerror="this.onerror=null;this.src='https://placehold.co/400x400?text=Nước+Hoa';">
        </div>
        <div class="w-full md:w-1/2 p-8 flex flex-col justify-center">
            <p id="modalBrand" class="text-gray-500 text-sm mb-2 uppercase tracking-widest"></p>
            <h2 id="modalName" class="text-3xl font-bold mb-4"></h2>
            <div class="flex flex-wrap gap-2 mb-6" id="modalTags"></div>
            <div class="flex items-center mb-6">
                <div class="star-rating" id="modalStars"></div>
                <span class="review-count" id="modalReviews"></span>
            </div>
            <div class="flex items-center mb-8">
                <span id="modalPrice" class="text-2xl font-bold text-black"></span>
                <span id="modalOldPrice" class="old-price text-lg"></span>
            </div>
            <button class="bg-black text-white py-4 px-8 rounded-full font-bold hover:bg-gray-800 transition-colors w-full md:w-fit">
                Thêm vào giỏ hàng
            </button>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        const products = [
            {
                id: 1,
                brand: "Chanel",
                name: "Chanel N5 Eau de Parfum",
                tags: ["Hoa", "Aldehyde"],
                price: 3500000,
                oldPrice: 4500000,
                discount: "-15%",
                rating: 4.5,
                reviews: 342,
                gender: "Nữ",
                image: "https://images.unsplash.com/photo-1541643600914-78b084683601?w=500&auto=format",
                date: "2024-01-01"
            },
            {
                id: 2,
                brand: "Dior",
                name: "Dior Sauvage Eau de Toilette",
                tags: ["Bergamot", "Tiêu"],
                price: 2800000,
                oldPrice: 3200000,
                discount: "-12%",
                rating: 4.8,
                reviews: 523,
                gender: "Nam",
                image: "https://images.unsplash.com/photo-1592945403244-b3fbafd7f539?w=500&auto=format",
                date: "2024-02-15"
            },
            {
                id: 3,
                brand: "Tom Ford",
                name: "Tom Ford Black Orchid",
                tags: ["Hoa lan", "Socola"],
                price: 4500000,
                oldPrice: null,
                discount: null,
                rating: 4.6,
                reviews: 340,
                gender: "Unisex",
                image: "https://images.unsplash.com/photo-1590540179852-2110a54f813a?w=500&auto=format",
                date: "2023-11-20"
            },
            {
                id: 4,
                brand: "Burberry",
                name: "Burberry Her Eau de Parfum",
                tags: ["Hoa", "Dâu tây"],
                price: 2700000,
                oldPrice: null,
                discount: null,
                rating: 4.7,
                reviews: 1382,
                gender: "Nữ",
                image: "https://images.unsplash.com/photo-1594035910387-fea47794261f?w=500&auto=format",
                date: "2024-03-01"
            },
            {
                id: 5,
                brand: "Prada",
                name: "Prada Luna Rossa Carbon",
                tags: ["Lavender", "Gỗ"],
                price: 3300000,
                oldPrice: null,
                discount: null,
                rating: 4.5,
                reviews: 476,
                gender: "Nam",
                image: "https://images.unsplash.com/photo-1585232351009-aa87416fca90?w=500&auto=format",
                date: "2024-02-10"
            },
            {
                id: 6,
                brand: "Marc Jacobs",
                name: "Marc Jacobs Daisy",
                tags: ["Dâu tây", "Violet"],
                price: 2500000,
                oldPrice: 2800000,
                discount: "-10%",
                rating: 4.4,
                reviews: 215,
                gender: "Nữ",
                image: "https://images.unsplash.com/photo-1557170334-a9632e77c6e4?w=500&auto=format",
                date: "2023-12-05"
            }
        ];

        const brands = ["Armani", "Ambroxan", "Bergamot", "Bạc hà", "Cam", "Cam chanh", "Chanel", "Dior", "Tom Ford", "Burberry", "Prada", "Marc Jacobs"];

        let filteredProducts = [...products];

        function renderBrands() {
            const brandFilters = document.getElementById('brandFilters');
            brandFilters.innerHTML = brands.map(brand => `
                <label class="flex items-center space-x-3 cursor-pointer">
                    <input type="checkbox" name="brand" value="${brand}" class="form-checkbox h-4 w-4 rounded border-gray-300">
                    <span class="text-gray-700">${brand}</span>
                </label>
            `).join('');
        }

        function formatPrice(price) {
            return new Intl.NumberFormat('vi-VN', { style: 'currency', currency: 'VND' }).format(price).replace('₫', 'đ');
        }

        function renderStars(rating) {
            let stars = '';
            for (let i = 1; i <= 5; i++) {
                if (i <= rating) {
                    stars += '<i class="fa-solid fa-star"></i>';
                } else if (i - 0.5 <= rating) {
                    stars += '<i class="fa-solid fa-star-half-stroke"></i>';
                } else {
                    stars += '<i class="fa-regular fa-star"></i>';
                }
            }
            return stars;
        }

        function renderProducts() {
            const grid = document.getElementById('productGrid');
            grid.innerHTML = filteredProducts.map(product => `
                <div class="product-card p-4 relative cursor-pointer" onclick="openQuickView(${product.id})">
                    ${product.discount ? `<div class="discount-badge">${product.discount}</div>` : ''}
                    <div class="h-48 flex items-center justify-center mb-4">
                        <img src="${product.image}" alt="${product.name}" class="max-h-full object-contain" onerror="this.onerror=null;this.src='https://placehold.co/400x400?text=Nước+Hoa';">
                    </div>
                    <div class="space-y-1">
                        <p class="text-gray-400 text-xs uppercase">${product.brand}</p>
                        <h3 class="font-bold text-sm truncate">${product.name}</h3>
                        <div class="flex flex-wrap gap-1 mt-2">
                            ${product.tags.map(tag => `<span class="tag">${tag}</span>`).join('')}
                        </div>
                        <div class="flex items-center mt-2">
                            <div class="star-rating">${renderStars(product.rating)}</div>
                            <span class="review-count">(${product.reviews})</span>
                        </div>
                        <div class="mt-2">
                            <span class="price">${formatPrice(product.price)}</span>
                            ${product.oldPrice ? `<span class="old-price">${formatPrice(product.oldPrice)}</span>` : ''}
                        </div>
                    </div>
                </div>
            `).join('');
        }

        function applyFilters() {
            const maxPrice = parseInt(document.getElementById('priceRange').value);
            const selectedGenders = Array.from(document.querySelectorAll('input[name="gender"]:checked')).map(cb => cb.value);
            const selectedBrands = Array.from(document.querySelectorAll('input[name="brand"]:checked')).map(cb => cb.value);
            const sortType = document.getElementById('sortSelect').value;
            const searchTerm = document.querySelector('input[placeholder="Tìm kiếm nước hoa theo tên,thương hiệu..."]')?.value.toLowerCase() || "";

            filteredProducts = products.filter(p => {
                const matchesPrice = p.price <= maxPrice;
                const matchesGender = selectedGenders.length === 0 || selectedGenders.includes(p.gender);
                const matchesBrand = selectedBrands.length === 0 || selectedBrands.includes(p.brand);
                const matchesSearch = p.name.toLowerCase().includes(searchTerm) || p.brand.toLowerCase().includes(searchTerm);
                return matchesPrice && matchesGender && matchesBrand && matchesSearch;
            });

            // Sorting
            if (sortType === 'price-asc') {
                filteredProducts.sort((a, b) => a.price - b.price);
            } else if (sortType === 'price-desc') {
                filteredProducts.sort((a, b) => b.price - a.price);
            } else if (sortType === 'name-asc') {
                filteredProducts.sort((a, b) => a.name.localeCompare(b.name));
            } else if (sortType === 'newest') {
                filteredProducts.sort((a, b) => new Date(b.date) - new Date(a.date));
            }

            renderProducts();
        }

        function openQuickView(id) {
            const product = products.find(p => p.id === id);
            if (!product) return;

            document.getElementById('modalImage').src = product.image;
            document.getElementById('modalBrand').textContent = product.brand;
            document.getElementById('modalName').textContent = product.name;
            document.getElementById('modalTags').innerHTML = product.tags.map(tag => `<span class="tag text-sm">${tag}</span>`).join('');
            document.getElementById('modalStars').innerHTML = renderStars(product.rating);
            document.getElementById('modalReviews').textContent = `(${product.reviews} đánh giá)`;
            document.getElementById('modalPrice').textContent = formatPrice(product.price);
            document.getElementById('modalOldPrice').textContent = product.oldPrice ? formatPrice(product.oldPrice) : '';
            
            document.getElementById('quickViewModal').classList.remove('hidden');
            document.getElementById('quickViewModal').classList.add('flex');
            document.body.style.overflow = 'hidden';
        }

        document.getElementById('closeModal').addEventListener('click', () => {
            document.getElementById('quickViewModal').classList.add('hidden');
            document.getElementById('quickViewModal').classList.remove('flex');
            document.body.style.overflow = 'auto';
        });

        document.getElementById('priceRange').addEventListener('input', (e) => {
            document.getElementById('priceLabel').textContent = formatPrice(e.target.value);
            applyFilters();
        });

        document.querySelectorAll('input[name="gender"], input[name="brand"]').forEach(el => {
            el.addEventListener('change', applyFilters);
        });

        document.getElementById('sortSelect').addEventListener('change', applyFilters);

        // Global search listener
        const searchInput = document.querySelector('input[placeholder="Tìm kiếm nước hoa theo tên,thương hiệu..."]');
        if (searchInput) {
            searchInput.addEventListener('input', applyFilters);
        }

        // Initialize
        renderBrands();
        renderProducts();

        // Re-attach search listener after dynamic changes if needed
        document.addEventListener('change', (e) => {
            if (e.target.name === 'brand') applyFilters();
        });
    </script>
}
